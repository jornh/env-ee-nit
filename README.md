

Companion for <https://github.com/dhth/envee/> to init (and update) the versions.toml file

Queries a K8s namespace for deployments and extracts image names and tags. The `versions.toml` file is updated accordingly

See the justfile
